const initialState = {
    isFetching: false,
    error: '',
    summoner: null
}

export const reducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}